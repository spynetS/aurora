[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aurora"
version = "0.1.0"
description = "Aurora package update assistant"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]

